<table border=1>
  <tr>
    <td>Team</td>
    <td>ELO</td>
  </tr>

  <% @teams.first(20).each do |t| %>
    <tr>
      <td><%= t.name %></td>
      <td><%= t.elo_cache %></td>
    </tr>
  <% end %>
</table>
